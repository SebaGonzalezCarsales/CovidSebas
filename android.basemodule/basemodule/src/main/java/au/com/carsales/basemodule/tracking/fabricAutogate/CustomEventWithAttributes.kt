package au.com.carsales.basemodule.tracking.fabricAutogate

/**
 * The type Custom event with attributes.
 */
class CustomEventWithAttributes

/**
 * Instantiates a new Cutom event with attributes.
 *
 * @param eventName  the event name
 * @param attributes the attributes
 */
/*
(eventName: String, attributes: Map<String, Any?>?) : CustomEvent(eventName) {

    init {
//        Log.e("Crash - EventSend = ", eventName)
        setAttributesForEvent(attributes)
    }


    private fun setAttributesForEvent(attributes: Map<String, Any?>?) {
        attributes?.let {

            for ((key, value) in attributes) {
//            Log.e("Crash - Key Attributes = ", key)
//            Log.e("Crash - Value Attributes = ", value)
                if (value is Number)
                    //putCustomAttribute(key, value)
                else if (value is String)
                    //putCustomAttribute(key, value)
            }
        }


    }
}
*/